language: node_js
sudo: false
node_js:
  - 10
  
addons:
  sonarcloud:
    organization: "hlastras-github"
script:
  # other script steps might be done before running the actual analysis
  - sonar-scanner
